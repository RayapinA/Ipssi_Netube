{% extends 'base.html.twig' %}

{% block title %} Netube {% endblock %}

{% block body %}
<div class="example-wrapper">
    <h1> Bienvenue sur Netube </h1>

    {% for video in videos %}
        <p> {{ video.title }}    <a href="{{ path("videoById",{'id':video.id}) }}"> Show </a> </p>
    {% endfor %}

</div>
{% endblock %}
